# =============================================================================
# NoteHub Environment Configuration - Hetzner VPS
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# Flask Configuration (generate with: openssl rand -hex 32)
FLASK_SECRET=your-super-secret-key-change-this

# Admin credentials (CHANGE THESE!)
NOTES_ADMIN_USERNAME=admin
NOTES_ADMIN_PASSWORD=YourSecurePassword123!

# MySQL Database (local container)
MYSQL_ROOT_PASSWORD=change-this-root-password
MYSQL_USER=notehub
MYSQL_PASSWORD=change-this-password
MYSQL_DATABASE=notehub

# CAPTCHA: "simple" (built-in), "recaptcha", or "none"
CAPTCHA_TYPE=simple
