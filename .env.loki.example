# =============================================================================
# Grafana Loki Environment Configuration
# =============================================================================
#
# This file contains environment variables for Grafana Loki setup.
# Copy this file to .env.loki and customize as needed.
#
# IMPORTANT: 
# - Uses main .env file's DOMAIN and ACME_EMAIL for Traefik integration
# - Accessible via https://logs.${DOMAIN} (Traefik) or http://localhost:3001 (direct)
# - Separate from monitoring Grafana (monitoring.${DOMAIN})
#
# =============================================================================

# Loki Grafana Admin Credentials (separate from monitoring Grafana)
LOKI_GRAFANA_ADMIN_USER=admin
LOKI_GRAFANA_ADMIN_PASSWORD=admin

# Loki Grafana Traefik Configuration
# Uses DOMAIN from main .env file (e.g., example.com)
# Default subdomain: logs.${DOMAIN}
# Override with custom router rule if needed:
# LOKI_GRAFANA_ROUTER_RULE=Host(`loki.example.com`)

# Loki Grafana Server Configuration
# LOKI_GRAFANA_ROOT_URL is auto-configured to https://logs.${DOMAIN}
# LOKI_GRAFANA_DOMAIN is auto-configured to logs.${DOMAIN}
# Override only if using custom subdomain:
# LOKI_GRAFANA_ROOT_URL=https://loki.example.com
# LOKI_GRAFANA_DOMAIN=loki.example.com

# Grafana Plugins (optional)
GRAFANA_INSTALL_PLUGINS=

# Loki Configuration (Advanced - usually not needed)
# LOKI_RETENTION_PERIOD=720h # 30 days
# LOKI_MAX_QUERY_LENGTH=721h # 30 days
