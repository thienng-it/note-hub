# Backend Environment Configuration for Firebase Frontend

# CORS - Allow Firebase Hosting domains
CORS_ORIGIN=https://note-hub-80f76.web.app,https://note-hub-80f76.firebaseapp.com,http://localhost:5173

# Server Port
PORT=5000

# JWT Secrets (CHANGE THESE!)
JWT_SECRET=your-super-secret-jwt-key-change-this
REFRESH_TOKEN_SECRET=your-super-secret-refresh-token-key-change-this

# Admin Account
NOTES_ADMIN_PASSWORD=your-secure-admin-password

# Database (SQLite for dev, MySQL for production)
# DATABASE_URL=mysql://user:password@host:3306/notehub

# Optional: Redis Cache
# REDIS_URL=redis://localhost:6379

# Optional: Elasticsearch
# ELASTICSEARCH_NODE=http://localhost:9200

# Optional: Google OAuth
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_REDIRECT_URI=https://your-backend-url.com/api/v1/auth/google/callback

# Environment
NODE_ENV=production
